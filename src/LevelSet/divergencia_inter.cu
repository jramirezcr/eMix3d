#include<cuda.h>
#include"divergencia.h"

void divergencia(
                double* const e_f,
                double* const f_f,
                double* const g_f,
                const double* const um,
                const double* const velocity,
                const double* const jbn_p,
                const double* const jbn_n,
                const unsigned int Nx, const unsigned int Ny, const unsigned int Nz,
                )
{

   return;
}

